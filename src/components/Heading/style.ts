import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  heading: {
    fontSize: 20,
    color: "#000000",
    marginVertical: 32,
    fontWeight: "bold",
  },
});

export default styles;
